app:
  name: example-app
  version: 1.0.0
  description: Example application using config plugin

server:
  host: 0.0.0.0
  port: 8080
  timeout: 30s

database:
  host: localhost
  port: 5432
  name: testdb
  user: testuser
  password: testpass
  pool:
    max_size: 10
    min_size: 2

debug: false

features:
  - feature1
  - feature2
  - feature3
